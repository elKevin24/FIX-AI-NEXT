# Example environment variables for local/dev
# PostgreSQL connection used by Prisma
DATABASE_URL=postgresql://user:password@localhost:5432/fix_ai_next?schema=public
# Optional direct URL (used for shadow or direct connections)
DIRECT_DATABASE_URL=postgresql://user:password@localhost:5432/fix_ai_next_direct?schema=public

# Other env vars used by the app (examples)
# NEXTAUTH_SECRET=your_nextauth_secret
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
