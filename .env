# ====================
# LOCAL DEVELOPMENT
# ====================
# Este archivo (.env) es para desarrollo local
# Para producción, Vercel usa las variables de entorno configuradas en el dashboard

# Database - Neon (Cloud PostgreSQL)
# ⚠️ IMPORTANTE: Para migraciones SIEMPRE usa la conexión DIRECTA (non-pooled)
# Las conexiones pooled no soportan advisory locks que Prisma necesita para migraciones
DATABASE_URL="postgresql://neondb_owner:npg_l3O0mWGqFBCY@ep-gentle-hill-adon7ba3.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Database - Local PostgreSQL (via Docker) - Deshabilitado
# DATABASE_URL="postgresql://workshop_user:workshop_pass@localhost:5432/workshop_db?schema=public"

# NextAuth v5
AUTH_SECRET="insecure-fallback-secret-for-development"
AUTH_URL="http://localhost:3000"

# ====================
# NOTES
# ====================
# - La base de datos ahora está configurada para usar Neon (cloud)
# - Para volver a usar Docker local, comenta la línea de Neon y descomenta la de Docker
# - No commitees este archivo al repositorio (está en .gitignore)