// Empty stub aliased for Vitest. `server-only`/`client-only` throw when imported
// outside a Next.js Server/Client Component build context; tests run in plain
// node/jsdom, so resolve them to a no-op module.
export {};
